{
  "name": "pixr",
  "version": "0.1.0",
  "description": "Installable Gemini image-generation CLI with reusable local defaults.",
  "type": "module",
  "bin": "src/cli.js",
  "license": "ISC",
  "scripts": {
    "dev": "node src/cli.js",
    "pixr": "node src/cli.js",
    "test": "node --test",
    "check": "node --test"
  },
  "keywords": [
    "cli",
    "gemini",
    "image-generation",
    "image-editor",
    "pixr"
  ],
  "engines": {
    "node": ">=20.10.0"
  },
  "dependencies": {
    "@google/genai": "^1.49.0",
    "sharp": "^0.34.5"
  }
}
